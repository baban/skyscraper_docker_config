---
apiVersion: v1
kind: Service
metadata:
  name: publify
spec:
  type: ClusterIP
  ports:
  - name: "publify-port"
    protocol: 'TCP'
    port: 3000
    targetPort: 3000
  selector:
    app: skyscraper-app
status:
  loadBalancer: {}
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: publify
spec:
  replicas: 2
  selector:
    matchLabels:
      app: skyscraper-app
  template:
    metadata:
      labels:
        app: skyscraper-app
    spec:
      containers:
        - name: skyscraper-publify
          image: babanban/skyscraper_publifyy
          ports:
            - containerPort: 3000
          env:
            - name: RAILS_ENV
              value: "production"
